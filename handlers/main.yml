---
# handlers file for roundcubemail
- name: restart httpd
  service:
    name: "{{ item }}"
    state: restarted
  with_items:
    - "{{ httpd_configuration[ansible_distribution]['httpd_service'] }}"
  when:
    - ansible_virtualization_type != "docker"
